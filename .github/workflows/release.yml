name: Release

on:
  push:
    tags: [v*]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

<<<<<<< HEAD
      - name: Install dependencies
        run: go mod tidy

      - name: Build binaries
        run: |
          mkdir dist
          GOOS=linux GOARCH=amd64 go build -o dist/yiff-linux-amd64 ./cmd/cli
          GOOS=darwin GOARCH=amd64 go build -o dist/yiff-darwin-amd64 ./cmd/cli
          GOOS=windows GOARCH=amd64 go build -o dist/yiff-windows-amd64.exe ./cmd/cli

      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: yiff-binaries
          path: dist/yiff-*
          if-no-files-found: error
=======
    - name: Build binaries
      run: |
        mkdir dist
        GOOS=linux GOARCH=amd64 go build -o dist/yiff-linux-amd64 ./cmd/cli
        GOOS=darwin GOARCH=amd64 go build -o dist/yiff-darwin-amd64 ./cmd/cli
        GOOS=windows GOARCH=amd64 go build -o dist/yiff-windows-amd64.exe ./cmd/cli
    
    - name: Upload compiled binaries as artifacts for later use
      uses: actions/upload-artifact@v4.6.2
      id: artifact-upload-step
      with:
	name: yiff-binaries-${{ github.ref }}
	path: dist/yiff*
        if-no-files-found: "error"
        overwrite: true
>>>>>>> 40ece24 (fix(ci): working on full release.yml workflow)

  changelog:

    name: Generate changelog
    runs-on: ubuntu-latest
    outputs:
      release_body: ${{ steps.git-cliff.outputs.content }}
      binaries-artifact-id: ${{ steps.artifact-upload-step.outputs.artifact-id }}
    steps:

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate a changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          # config: cliff.toml
          args: -vv --latest --strip header
        env:
          OUTPUT: CHANGES.md
          GITHUB_REPO: ${{ github.repository }}

<<<<<<< HEAD
      - name: Upload the binary releases
        uses: svenstaro/upload-release-action@v2
        id: artifact-upload-step
        with:
          name: yiff-binaries-${{ github.ref }}
          file: dist/yiff*
          file_glob: true
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref }}
          body: ${{ steps.git-cliff.outputs.content }}

=======
>>>>>>> 40ece24 (fix(ci): working on full release.yml workflow)
  release:

    name: Create Release
    runs-on: ubuntu-latest
    needs: [build, changelog]
    steps:
<<<<<<< HEAD

      - uses: actions/download-artifact@v4
=======
      - name: Fetch compiled binaries from last job
        uses: actions/download-artifact@v4
>>>>>>> 40ece24 (fix(ci): working on full release.yml workflow)
        with:
          name: yiff-binaries-${{ github.ref }}

      - name: Display structure of downloaded files
        run: ls -R

      - name: Upload the binary releases
        uses: svenstaro/upload-release-action@v2
        env:
          OUTPUT1: ${{needs.build.outputs.output1}}
        with:
          file: dist/yiff*
          file_glob: true
          tag: ${{ github.ref }}
<<<<<<< HEAD
          body: ${{ needs.changelog.outputs.release_body }}

=======
          body: ${{ needs.changelog.outputs.release_body }}    
>>>>>>> 40ece24 (fix(ci): working on full release.yml workflow)
